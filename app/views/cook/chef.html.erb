<h1>Cook#chef</h1>
<p><%= @title%></p>

<% @all_my_chefs.each do |the_chefs|
  <%= the_chefs.first_name %>
  the_chefs.last_name
  the_chefs.age

<% end %>



<%# def hey
    @name = params[:name]
    respond_to do |format|
      format.html
      format.json do
        render :json => { message: "Hey, #{@name} you are in a json page"}
      end
    end
  end %>
